let numero = 8
let contador = 0;

console.log("a tabuada de multiplicação n°: " +numero);

while (contador <= 10) {
    let resultado = numero * contador;
    console.log(numero + "x" + contador+ "=" +resultado);
    contador++;
}